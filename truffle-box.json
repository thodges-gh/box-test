{
  "ignore": [
    "README.md",
    ".gitignore",
    "core",
    "examples"
  ],
  "commands": {
    "Compile contracts": "npm run compile",
    "Run tests": "npm test",
    "Migrate to development network": "npm run migrate:dev",
    "Migrate to live network": "npm run migrate:live"
  },
  "hooks": {
    "post-unpack": "bash unbox.sh && npm install"
  }
}